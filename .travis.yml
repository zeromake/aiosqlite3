language: python

python:
    - 3.5
    - 3.6

install:
    - pip install pipenv -i https://pypi.doubanio.com/simple/
    - pipenv install -d

script:
    - make test
    - make cov

after_success:
    - codecov